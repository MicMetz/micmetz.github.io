{
		"name": "micmetz.github.io",
		"version": "0.1.0",
		"private": true,
		"repository": {
				"type": "git",
				"url": "https://github.com/MicMetz/micmetz.github.io.git",
				"private": false
		},
		"scripts": {
				"dev": "next dev",
				"build": "next build && next export",
				"start": "next start",
				"export": "next export",
				"deploy": "next build && touch ./out/.nojekyll && git add ./out/ && git commit -m \"Deploy\" && git subtree push --prefix ./out origin gh-pages"
		},
		"dependencies": {
				"cssnano": "^6.0.1",
				"gh-pages": "^5.0.0",
				"next": "^13.4.2",
				"react": "^18.2.0",
				"react-dom": "^18.2.0",
				"react-icons": "^4.8.0",
				"react-is": "^18.2.0",
				"styled-components": "^5.3.5",
				"styled-normalize": "^8.0.7"
		}
}
